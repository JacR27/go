package main

import (
       "fmt"
       "os"
       "strings"
)

func main() {
     s, sep := "", ""
     var i int
     for i, s = range(os.Args[1:])
     
     sep = " "
     fmt.Println(strings.Join(os.Args[0:]," "))
}